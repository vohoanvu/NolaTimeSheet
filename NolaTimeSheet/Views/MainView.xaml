<UserControl 
    x:Class="NolaTimeSheet.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
    xmlns:viewModels="clr-namespace:NolaTimeSheet.ViewModels"
    mc:Ignorable="d" 
    d:DesignHeight="500" 
    d:DesignWidth="500">

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding FetchUserCommand}"/>
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <ComboBox Width="160" 
                  ItemsSource="{Binding AppUsers}"
                  DisplayMemberPath="UserName" 
                  SelectedValuePath="Id"
                  SelectionChanged="UserOnChange" 
                  Height="25" Margin="162,56,178,419" Text="Select a User"/>
        <Label Content="NOLA Timesheet Entries" HorizontalAlignment="Center" Margin="0,20,0,0" VerticalAlignment="Top" Width="326" HorizontalContentAlignment="Center" FontFamily="Arial Black" FontSize="22"/>
    </Grid>
</UserControl>