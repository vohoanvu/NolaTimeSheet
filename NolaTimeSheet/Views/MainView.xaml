<UserControl 
    x:Class="NolaTimeSheet.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
    xmlns:viewModels="clr-namespace:NolaTimeSheet.ViewModels"
    mc:Ignorable="d" 
    d:DesignHeight="500" 
    d:DesignWidth="500">

    <UserControl.DataContext>
        <viewModels:MainViewModel/>
    </UserControl.DataContext>

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding FetchProjectCommand}"/>
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <ComboBox Width="160" 
                  ItemsSource="{Binding ProjectsVm}"
                  DisplayMemberPath="Name" 
                  SelectionChanged="ProjectOnChange" 
                  Height="25"/>
    </Grid>
</UserControl>